version: '2'
services:
  db:
    build: oracle/
    restart: always
  candlepin:
    build: candlepin-base/
    environment:
      USING_ORACLE: "true"
      DBHOSTNAME: db
    privileged: true
      volumes:
        - ../:/candlepin-dev
    depends_on:
      - db
